// map.js — tile data load/render + collision.
// Implemented during the jam window. See GDD §9 (tileset + tilemap JSON).
export function createMap() {
  // TODO(jam): load tilemap JSON, render visible tiles, per-tile + per-object collision
  return {};
}
