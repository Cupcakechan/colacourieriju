// camera.js — follows the player, clamped to map bounds.
// Implemented during the jam window. See GDD §6.
export function createCamera() {
  // TODO(jam): follow target, clamp to map size, expose world→screen offset
  return {};
}
