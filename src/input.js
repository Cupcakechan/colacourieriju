// input.js — keyboard state (which keys are currently held).
// Implemented during the jam window. See GDD §5 (Controls).
export function createInput() {
  // TODO(jam): listen to keydown/keyup, expose held-key state for 8-dir movement + jump
  return {};
}
