// player.js — the Iju courier: position, movement, cargo state, animation.
// Implemented during the jam window. See GDD §4.1 / Slice 0.
export function createPlayer() {
  // TODO(jam): position/velocity, free 8-dir movement, cargo state (empty-handed/carrying), animation
  return {};
}
