// ui.js — fizz bar, score, grade popup, destination arrow.
// Implemented during the jam window. See GDD §6.
export function createUI() {
  // TODO(jam): draw fizz bar, score, streak, grade popups, off-screen destination arrow
  return {};
}
