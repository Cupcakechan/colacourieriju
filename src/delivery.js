// delivery.js — pickup/dropoff zones, fizz grade, score, streak, shift timer.
// Implemented during the jam window. See GDD §4.2–4.5.
export function createDelivery() {
  // TODO(jam): pickup/dropoff zones, fizz update, grade on delivery, score + streak, shift timer
  return {};
}
